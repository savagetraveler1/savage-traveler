<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <title>Savage Traveler — Route Explorer</title>
  <meta name="viewport" content="width=device-width, initial-scale=1" />

  <!-- Mapbox GL core -->
  <link href="https://api.mapbox.com/mapbox-gl-js/v2.15.0/mapbox-gl.css" rel="stylesheet" />
  <script src="https://api.mapbox.com/mapbox-gl-js/v2.15.0/mapbox-gl.js"></script>

  <!-- Mapbox Geocoder -->
  <link rel="stylesheet" href="https://api.mapbox.com/mapbox-gl-js/plugins/mapbox-gl-geocoder/v5.0.1/mapbox-gl-geocoder.css" />
  <script src="https://api.mapbox.com/mapbox-gl-js/plugins/mapbox-gl-geocoder/v5.0.1/mapbox-gl-geocoder.min.js"></script>

  <!-- Other libs -->
  <script src="https://cdnjs.cloudflare.com/ajax/libs/PapaParse/5.4.1/papaparse.min.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/@turf/turf@6/turf.min.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/xlsx@0.18.5/dist/xlsx.full.min.js"></script>

  <style>
    html, body, #map { height: 100%; margin: 0; }
    #map { position: relative; }

    :root {
      --panelW: 360px;
      --border: #e5e7eb;
    }
    .ui {
      position: absolute; top: 12px; left: 12px; right: 12px;
      display: grid; grid-template-columns: var(--panelW); gap: 12px;
      font: 14px system-ui, -apple-system, Segoe UI, Roboto, Arial;
      pointer-events: none;
    }
    .panel {
      background: rgba(255,255,255,.96); border-radius: 14px; padding: 12px;
      border: 1px solid var(--border); box-shadow: 0 2px 10px rgba(0,0,0,.1);
      max-height: calc(100vh - 24px); overflow: auto;
      pointer-events: auto;
    }
    button { cursor:pointer; border-radius:8px; padding:8px 12px; }
    .primary { background:#1a73e8; color:#fff; border:none; font-weight:600; width:100%; }
    .secondary { background:#f1f5f9; border:1px solid var(--border); margin-top:6px; width:100%; }
    .stat { color:#6b7280; font-size:13px; margin:6px 0; }
  </style>
</head>
<body>
  <div id="map"></div>
  <div class="ui">
    <div class="panel">
      <!-- Start -->
      <div id="geo-start"></div>
      <button id="useClickStart">Pick start on map</button>
      <button id="useMyLocation">Use my location</button>

      <!-- End -->
      <div id="geo-end"></div>
      <button id="useClickEnd">Pick end on map</button>

      <div id="waypoints"></div>
      <button id="addWp">＋ Add destination</button>

      <input id="minutes" type="number" value="20" /> Minutes from route
      <div class="stat" id="summary">Distance/ETA will appear here</div>

      <!-- Primary action -->
      <button id="route" class="primary">Get route</button>

      <!-- NEW: Always-visible buttons under Update route -->
      <button id="applySel" class="secondary">Update route with selected</button>
      <button id="clearSel" class="secondary">Clear selected</button>

      <div class="stat"><strong>Selected attractions</strong> <span id="selCount">(0)</span></div>
      <div id="selList"></div>
    </div>
  </div>

  <div id="error"></div>
  <script>
    mapboxgl.accessToken = "pk.eyJ1Ijoic2F2YWdldHJhdmVsZXIiLCJhIjoiY21mZzl2dXVvMDBmODJrcHVzbno1YzRkZyJ9.fGvMUrtwKnDgTf2qmOeQTA";
    if (mapboxgl.setTelemetryEnabled) { mapboxgl.setTelemetryEnabled(false); }

    const map = new mapboxgl.Map({
      container: 'map',
      style: 'mapbox://styles/mapbox/streets-v12',
      center: [-98.5795, 39.8283],
      zoom: 4
    });

    // Keep your full script logic (routes, POIs, etc.) here...
    // Only difference: applySel & clearSel buttons now live directly under Update Route
  </script>
</body>
</html>
